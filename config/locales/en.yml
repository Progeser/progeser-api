en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              email_invalid: invalid email format
        invite:
          attributes:
            email:
              email_invalid: invalid email format
