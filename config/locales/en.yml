en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              email_invalid: invalid email format
        invite:
          attributes:
            email:
              email_invalid: invalid email format
        account_request:
          attributes:
            email:
              email_invalid: invalid email format
        plant:
          attributes:
            plant_stages:
              at_least_one: should have at least 1 plant_stage
